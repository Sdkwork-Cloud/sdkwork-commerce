{
  "name": "@sdkwork/commerce-contracts",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Canonical SDKWork commerce contracts for account, wallet, points, membership, ledger, prehold, API, database, and Java/Rust parity.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "pnpm --dir ../../../.. exec vitest run packages/common/commerce/sdkwork-commerce-contracts/tests --config vitest.config.ts --configLoader native --pool vmThreads"
  },
  "sdkwork": {
    "architecture": "common",
    "capability": "commerce-contracts",
    "domain": "commerce",
    "packageType": "contracts",
    "status": "standard"
  }
}
