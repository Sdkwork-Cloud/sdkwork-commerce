[package]
name = "sdkwork_commerce_core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "SDKWork canonical commerce domain, context, and amount contracts for local/private Rust deployments."

[lib]
path = "src/lib.rs"
